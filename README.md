# Gatsby Starter Taillwind and styled-components

Yes styled-components with Taillwind classes, why? Why not 😛

This uses the `tailwind.macro` which is used with `babel-plugin-tailwind-components`.